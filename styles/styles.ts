import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  map: {
    width: "100%",
    height: "90%", 
  },
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "flex-start",
  },
});
